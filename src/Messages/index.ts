import { status } from "./system/status";
import { sendMessage as send } from "./sendMessage";
import { editMessage as edit } from "./editMessage";

export const message = {
  send,
  edit,
  status,
};
