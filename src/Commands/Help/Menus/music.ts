import { MessageEmbed } from 'discord.js';



export const music = {
    main: new MessageEmbed()
        .setTitle('Music Commands')
        .setColor('PURPLE')
        .setDescription('`Music Commands Coming Soon`')
        .setFooter('Type /help <CommandName> for details on a command'),
}