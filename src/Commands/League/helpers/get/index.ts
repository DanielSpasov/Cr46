import { championsCount, summonerName, serverName } from "./interactions";
import { latestVersion } from "./latestVersion";
import { champions } from "./champions";
import { summoner } from "./summoner";
import { rank } from "./rank";
import { url } from "./url";

export const get = {
  championsCount,
  latestVersion,
  summonerName,
  serverName,
  champions,
  summoner,
  rank,
  url,
};
